# Snapfleet — Image Prompts

**Accent:** `#F472B6` · **Repo:** [snapfleet](https://github.com/bensblueprints/snapfleet) · **Replaces:** Urlbox ($19/month) · **Price:** $39 one-time

> **Style block — prepend to every prompt below:**
> Premium dark-mode SaaS product visual, near-black background (#0B0E14), single accent color #F472B6 with soft neon glow, high contrast, subtle grid texture, clean modern sans-serif UI, soft shadows, cinematic rim lighting, crisp render, no text unless specified.

## Hero image (16:9)

A fleet of glowing pink camera-shutter apertures hovers in formation above a dark data plane, each aperture firing a beam down at a floating browser window and freezing it into a crisp photographic print that slides onto a conveyor of light — PNG, JPG, and PDF sheets stacking neatly into a glass cache vault. In the foreground a clean UI panel from the app's playground shows a URL field, a format selector, and a live preview thumbnail rimmed in #F472B6. Industrial-scale rendering, zero clouds — everything happens on one glowing local server block. Cinematic, premium dark.

## Dashboard mockup (16:10)

Stylized screenshot-like render of the Snapfleet playground: dark UI, pink #F472B6 accents. Left panel: parameter form with URL field "https://example.com", format toggle set to PNG, width 1280 and height 800 inputs, toggles for full_page and dark_mode (dark_mode on), a selector field, quality slider, and scale set to 2x. Right: a live rendered preview of a dark webpage inside a device-style frame, with a badge reading "X-Snapfleet-Cache: HIT" and a generated request URL plus a copyable curl one-liner beneath. Top nav: Playground, Gallery, API Keys, Stats; a pink "Capture" button glowing.

## OG / social card (1200×630)

Dark #0B0E14 card with subtle grid texture. "Snapfleet" set very large left in clean sans-serif with a soft #F472B6 glow. Below in white: "Self-hosted screenshot API — unlimited renders on your own server." Right side: a floating browser window being frozen into a photographic print by a pink shutter-aperture glyph, with small PNG / JPG / PDF chips trailing off it, neon rim light. Bottom-left: rounded badge with exact text "$39 once · no subscription", pink outline.

## Product Hunt gallery (5)

### 1. Playground hero
Dark playground screen: full parameter form on the left (URL, format, viewport, full-page, dark-mode, selector, scale), a live rendered preview of a real website on the right, and the generated curl command visible in a code strip below; #F472B6 accents, premium glow.

### 2. Dashboard
Snapfleet stats screen: four stat tiles — total requests, cache hit rate at a believable "78%", average render time "1.4s", storage used — above an hourly request bar chart in pink #F472B6; dark UI, clean spacing.

### 3. Gallery grid
A wall of cached screenshot thumbnails in a masonry grid — varied sites, light and dark pages — each with a small cache-state badge (HIT in pink, MISS in gray) and hover controls for re-take and delete; dark background, soft #F472B6 highlights.

### 4. API keys screen
API keys table, dark UI: rows with key names like "prod-og-images" and "staging", columns for rate limit and daily quota with values, usage counters, and revoke buttons; above, a freshly created key banner showing a truncated token starting with exact text "sf_" and a copy button glowing #F472B6.

### 5. Terminal shot
Side-by-side: a dark terminal with a single curl command fetching /api/v1/screenshot on the left, the resulting crisp PNG of a webpage materializing on the right; caption beneath in exact text "One endpoint. Your server. $0 per render."; pink #F472B6 rim light, cinematic.
