# Wrangle — Image Prompts

**Accent:** `#FACC15` · **Repo:** [wrangle-toolbox](https://github.com/bensblueprints/wrangle-toolbox) · **Replaces:** He3 ($9.90/month) · **Price:** $15 one-time

> **Style block — prepend to every prompt below:**
> Premium dark-mode SaaS product visual, near-black background (#0B0E14), single accent color #FACC15 with soft neon glow, high contrast, subtle grid texture, clean modern sans-serif UI, soft shadows, cinematic rim lighting, crisp render, no text unless specified.

## Hero image (16:9)

A heavy matte-black toolbox floats open in dark space, and instead of wrenches it holds ten glowing golden instruments of data: braces for JSON, a diff split-blade, a key-shaped JWT decoder, a hash stamp, a regex net, an hourglass timestamp. Streams of raw data enter one side tangled and exit the other side perfectly formatted, glowing #FACC15. A severed network cable lies in the foreground, cleanly cut and capped — nothing leaves this machine. Floating UI hint: the app sidebar listing ten tools with a converter panel showing JSON turning into YAML. Cinematic, premium dark.

## Dashboard mockup (16:10)

Stylized screenshot-like render of Wrangle's Convert tool: dark UI, golden #FACC15 accents. Left sidebar listing all 10 tools with icons — Convert, Validate & Format, JSONPath, Diff, JWT Decoder, Base64/URL, Hash, UUID, Regex Tester, Timestamp — Convert active. Main area split: left pane JSON input with a small user object ("name", "email", "plan": "pro"), format auto-detect chip reading "JSON"; right pane the same data as YAML output with a copy button; toolbar options for delimiter and indent, a swap-direction arrow glowing gold. A "100% offline" badge and Ctrl+K palette hint in the header; recent-input history chips along the bottom.

## OG / social card (1200×630)

Dark #0B0E14 card with subtle grid texture. "Wrangle" set very large left in clean sans-serif with a soft #FACC15 glow. Below in white: "10 offline dev data tools — stop pasting API keys into random websites." Right side: a floating toolbox glyph radiating ten small golden tool icons (braces, key, hash, dice, regex slash, clock), with a cleanly cut network cable beneath, neon rim light. Bottom-left: rounded badge with exact text "$15 once · no subscription", gold outline.

## Product Hunt gallery (5)

### 1. Hero
Full Wrangle window in dark mode: Convert tool open with JSON input on the left and YAML output on the right, the sidebar showing all 10 tool icons, and a visible "100% offline" badge in the header; #FACC15 accents, premium glow.

### 2. JWT decoder
The JWT Decoder tool: a pasted token split into color-coded header and payload panes of pretty JSON, a green pill reading exact text "expires in 2h 14m", and an HS256 verify field below; caption overlay in exact text "Your JWT contains prod credentials. Why are you pasting it into a website?"; dark UI, gold accents.

### 3. Regex tester
The Regex Tester: an email pattern in the pattern bar, sample text with matches highlighted in glowing #FACC15, a capture-group table below, and a replace-preview pane; a small shield chip noting the 2-second worker timeout guard; dark UI.

### 4. Smart JSON diff
The Diff tool in smart JSON mode: two JSON blobs with visibly shuffled key order and a calm badge reading exact text "No differences"; beneath, a second state where one changed value is highlighted green/red in a side-by-side split; gold accents, dark UI.

### 5. Ctrl+K palette
The command palette open over the Hash tool: a centered search field with tool results listed (Convert, Diff, UUID, Timestamp), the Hash workspace behind showing a file digest table with MD5/SHA-256 rows for a dropped file; caption in exact text "Ten tools, one keystroke apart."; #FACC15 selection glow, dark UI.
